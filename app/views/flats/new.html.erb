<h1 class="pb-5">Add a flat:</h1>
<%= simple_form_for [@flat] do |f| %>
  <%= f.input :name, required: true %>
  <%= f.input :description, as: :text %>
  <%= f.input :address %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
